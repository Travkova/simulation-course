### Моделирование случайных событий

**Часть 1:**  
Приложение «Скажи “да” или “нет”».

**Часть 2:**  
Приложение «Шар предсказаний» (Magic 8-Ball).

**Решение:**
Для лабораторной работы использовался мультипликативный конгруэнтный генератор псевдослучайных чисел из 4 лабораторной:
<img width="382" height="160" alt="image" src="https://github.com/user-attachments/assets/6777c1e0-b125-4ec5-93f6-ec08eabcfecd" />

**Теория для реализации приложения да/нет:**

Вероятность события можно представить геометрически как отрезок на интервале [0, 1]:

$$P\{\text{random} < p\} = p$$, где random - СВ, равномерно распределённая на интервале [0, 1], p - вероятность наступления события

Алгоритм: 
1. A := α (генерация СВ)
2. Если α < p, то событие наступило (ДА). Иначе: событие не наступило (НЕТ)

**Теория для реализации приложения шара предсказаний:**

События $A_1, A_2, ..., A_m$ образуют полную группу попарно несовместных событий, если:

$$\sum_{i=1}^{m} p_i = 1$$, где $p_i$ = P{Aᵢ} — вероятность наступления события $A_i$.

Алгоритм генерации события из группы событий:

1. A := α (генерация СВ); k := 1 (Счётчик, индекс текущего события)
2. A := A - pₖ
3. Если A ≤ 0 → произошло событие Aₖ
4. Иначе k := k + 1, повторить шаг 2

Также были вычислены эмпирические вероятности (относительных частот) для анализа сходимости к теоретическим вероятностям.

**Частота события $A_k$:**

$$n_k = \text{количество появлений события } A_k$$

**Объём выборки (общее число попыток):**

$$N = \sum_{k=1}^{m} n_k$$

где $m$ — количество событий в полной группе.

**Эмпирическая вероятность (относительная частота):**

$$\hat{p}_k = \frac{n_k}{N}$$

или, в альтернативной записи:

$$\tilde{p}_k = \frac{n_k}{N}$$

**Свойство нормировки:**

$$\sum_{k=1}^{m} \hat{p}_k = 1$$
